services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=lox
      - POSTGRES_USER=lox
      - POSTGRES_PASSWORD=lox
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
